spring:
  cloud:
    gateway:
      routes:
        - id: admin
          uri: http://localhost:2222 # Use the name of the microservice as registered in your service registry (e.g., Eureka)
          predicates:
            - Path=/admin/**
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
      

                  
